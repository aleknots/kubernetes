# Alternativa opcional ao App-of-Apps: gerar as 3 Aplicações a partir de uma lista.
# Use OU o app-root.yaml + applications/*.yaml OU este ApplicationSet (não ambos,
# para evitar conflitos com os mesmos nomes de Aplicação).
#
# Bootstrap:
#   kubectl apply -f kustomize-env-demo/argocd/applicationset.yaml
apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: app-banner
  namespace: argocd
spec:
  goTemplate: true
  goTemplateOptions: ["missingkey=error"]
  generators:
    - list:
        elements:
          - env: dev
            label: DEVELOPMENT
          - env: stg
            label: STAGING
          - env: prd
            label: PRODUCTION
  template:
    metadata:
      name: 'app-banner-{{.env}}'
    spec:
      project: lab
      source:
        repoURL: https://github.com/aleknots/Kubernetes.git
        targetRevision: main
        path: 'kustomize-env-demo/overlays/{{.env}}'
      destination:
        server: https://kubernetes.default.svc
        namespace: 'app-banner-{{.env}}'
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true
