apiVersion: v1
kind: Service
metadata:
  name: admin-site-service
  namespace: lab-app
  labels:
    app: admin-site
spec:
  type: ClusterIP
  selector:
    app: admin-site
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
